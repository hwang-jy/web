extends layout

block content
  link(rel='stylesheet', href='/stylesheets/auth.css')

  p 서비스를 이용하기 위해 로그인해야 합니다.

  div.login_wrap
    form(action="/auth/sign", method="POST" target="_top" autocomplete="off") 
      div
        input(type="text", name="id" class="input_text" placeholder="아이디" maxlength="40")
      div
        input(type="password", name="pw" class="input_text" placeholder="비밀번호" maxlength="16")
      input(type="submit", value="로그인" formaction="/" formmethod="post")
    
  ul.find_wrap
      li
        a.find_text(href="/auth/pw") 비밀번호 찾기
      li
        a.find_text(href="/auth/id") 아이디 찾기
      li
        a.find_text(href="/auth/sign") 회원가입

  
 

  